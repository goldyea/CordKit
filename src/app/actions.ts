"use server";

// Simple actions for the new single-password system
// Most authentication logic is now handled client-side

export const placeholder = () => {
  // Placeholder function to maintain file structure
  // All authentication is now handled in the PasswordGate component
};
